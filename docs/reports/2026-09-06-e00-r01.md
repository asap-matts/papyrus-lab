# Esperimento E00-R01 — controllo noto della catena su w035

Stato: **completato** — E00 superato (seed 42); seed 43 informativo eseguito e concordante. Revisione dell'esito da parte di Codex (read-only, 6 settembre 2026): verdetto "E00 superato a livello L3 sostenuto dal criterio preregistrato, dalle metriche e dagli hash"; 7 finding sulla scheda (2 P1, 5 P2), tutti accettati e integrati in questa versione: provenienza effettiva della label nei run GPU, tracciabilità dei log dei tentativi falliti, durata reale del setup, comandi risolti, rimozione di ipotesi causali non sostenute, formulazione della concordanza fra seed, registrazione esplicita del gate A.

## Domanda e criterio deciso prima della prova

- ID, autore, data: `E00-R01`; esecuzione automatizzata via API Kaggle pilotata da Claude (writer), approvazioni di Matteo; 6 settembre 2026.
- Ipotesi: la catena dati → codice → modello → output è collegata correttamente e con l'orientamento giusto; su un segmento del training set il modello separa nettamente inchiostro e sfondo.
- Baseline e singola modifica: nessuna modifica. Comando ufficiale del branch `merge-ink-pipelines` con tre scelte di esecuzione preregistrate (`--no-compile`, `--gpus 0`, `--batch-size 1`).
- Contributo esterno di riferimento: R01 `ScrollPrize/villa` @ `3ea17f5…`, model card `scrollprize/ink_9um` @ `7109667…`, dataset label `ink_9um`; nessun contributo community adottato.
- Budget massimo: 30 min di inferenza per tentativo (timeout interno) e 60 min di sessione (piattaforma); 10 GB di disco; nessuna spesa.
- Condizione di arresto: come da [piano](../plans/2026-09-06-e00-controllo-noto-w035.md) §7, tutte codificate come asserzioni nei notebook.
- Criterio per accettare o respingere: gate A (identità) tutto vero; gate B: AUROC sui pixel supervisionati ≥ 0,90 **e** AUROC con la label non trasformata strettamente massima rispetto a rotazione 180°, specchio Y, specchio X ([piano §5](../plans/2026-09-06-e00-controllo-noto-w035.md)).

## Dati e riproduzione

- Rotolo, volume, segmento: PHerc. 0139, surface volume nativo 9,362 µm `9.362um-1.2m-113keV-volume-20250728140407.zarr`, segmento pubblico `20260317000000-w035_2026031718`, livello 0 `[28, 5820, 5240]` uint8; maschera valida = `w035_supervision_mask` al piano Z=14 (1.071.121 pixel, di cui 334.035 d'inchiostro).
- Origine, licenza, revisione e SHA-256 degli input: open-data ufficiale (S3); label dal bucket ufficiale `ink_9um` (5.128 file, 737.833 byte, tree SHA-256 `09037f1d…1f27`), verificata per contenuto nel run. **Percorso effettivo nei run GPU: download diretto dal bucket (ripiego)**, in 252 s (seed 42) e 1.007 s (seed 43); il dataset Kaggle privato costruito da `scripts/build_w035_label_dataset.py` è stato usato solo nel preflight, perché nei run GPU Kaggle lo monta in `/kaggle/input/datasets/<utente>/<slug>/` e la ricerca del manifest, a profondità fissa, non lo trovava (ricerca resa ricorsiva dopo il run; non ancora collaudata su GPU). Stesso conteggio e stesso hash dell'albero in tutti i casi. Checkpoint MIT (model card), SHA-256 nel manifest.
- Provenienza del riferimento: annotazioni degli organizzatori trasferite sui volumi pubblici con revisione dell'offset caso per caso (README dataset). Regioni non supervisionate escluse dalle misure.
- Ruolo dei campioni: **controllo noto**. w035 è nel training set di `ink_9um` in due rappresentazioni (2,4 µm ridotta e nativa 9,362 µm).
- Evidenza di separazione dal training: **nessuna, per costruzione**. Questo esperimento non misura generalizzazione.
- Commit PapyrusLab `17ea887` (piano e notebook congelati; correzioni dei tentativi 1–2 incluse); commit villa `3ea17f54a9b3d5fd1aaf73e1d2c8386dbaa9f30e`; ScrollScout non usato.
- Checkpoint: `hybrid_3d2d-seed42/step-075000.pth` (SHA-256 `e635558a…9cab`) e `hybrid_3d2d-seed43/step-075000.pth` (`2aeaa85a…b28f`), revisione HF `7109667e…7bb1`. Seed di inferenza: non applicabile (inferenza deterministica salvo kernel CUDA).
- Hardware, ambiente e versioni: Kaggle, acceleratore T4 (allocazione 2 × Tesla T4 15 GiB, driver 580.159.04, **una sola visibile** via `CUDA_VISIBLE_DEVICES=0`), Python 3.12.13, PyTorch 2.10.0+cu128; Python di sistema con `pip install --no-deps` e tre pacchetti aggiunti dal lock (`asciitree 0.3.3`, `pynrrd 1.1.3`, `imagecodecs 2026.3.6`). Elenco completo nel [manifest](2026-09-06-e00-r01-manifest.json).
- Dimensioni, assi, scala, profondità, direzione, normalizzazione: ZYX, 9,362 µm isotropo, 17 slice centrali (indici 6–22, centro 14 = piano della label), direzione `forward`, preprocessing `tifxyz_robust` ricostruito dal checkpoint, autocast float16.
- Comando eseguito (risolto; `$ZARR` = `https://vesuvius-challenge-open-data.s3.amazonaws.com/PHerc0139/segments/20260317000000-w035_2026031718/surface-volumes/9.362um-1.2m-113keV-volume-20250728140407.zarr`, cwd `/tmp/e00`, `CUDA_VISIBLE_DEVICES=0`):
  - seed 42: `timeout -s INT -k 30 1800 python -m koine_machines.inference.infer "$ZARR" checkpoints/ink_9um/hybrid_3d2d-seed42/step-075000.pth /kaggle/working/e00/out/w035_seed42_step075000.tif --overlap 0.5 --blend-mode hann --no-compile --gpus 0 --batch-size 1`
  - seed 43: identico con `seed43` al posto di `seed42` nel checkpoint e nel nome del TIFF.
  Nessuna maschera (`No mask supplied. Using the entire zarr.`); 6.458 patch, copertura 82,1 % dallo scan di occupazione al livello 3.
- Artefatti: `runs/E00-R01/<run>/e00/{out,logs}` sul fisso (fuori da Git), scaricati da Kaggle con `scripts/kaggle_e00.py output` e verificati con `SHA256SUMS` (seed42: 23 file, 0 differenze); TIFF `w035_seed42_step075000.tif` 14.058.408 byte, SHA-256 `a0cc3f53…3c38`. Notebook generati in `kaggle/`, versioni Kaggle nel manifest.

## Risultati

- Qualità e metriche per caso rispetto alla baseline (seed 42, tentativo 3):

  | Misura | Valore |
  |---|---|
  | AUROC, label originale | **0,99908** |
  | AUROC, label ruotata 180° | 0,5319 |
  | AUROC, label specchiata Y | 0,5397 |
  | AUROC, label specchiata X | 0,5765 |
  | Orientamento | corretto (originale strettamente massima) |
  | Mediana predizione su inchiostro / sfondo | 200 / 57 (scala 0–255) |
  | **gate_A** (identità) | **superato**: commit villa `3ea17f5…` (`villa_commit.txt`), SHA-256 dei due checkpoint (`checkpoints_sha256.txt`), label 5128/737833 e tree hash (`label_count.txt`), indici 6–22 e `in_chans=17` (`infer_seed42.log`), `exit_code=0`, TIFF `(5820, 5240)` uint8 (`metrics_seed42.json`) |
  | **gate_B** | **superato** |

- Tempi (seed 42): fase precedente all'inferenza **5 min 50 s** (14:21:34 → 14:27:24), di cui 252 s di download della label in ripiego; **inferenza 200 s** (6.458 blocchi, ~32 blocchi/s); sessione 9 min 12 s; download degli output sul fisso pochi secondi. L'ambiente Kaggle si azzera a ogni run.
- Memoria, spazio, dati trasferiti: picco GPU 0 **383 MiB**, GPU 1 **3 MiB** (campionamento ogni 5 s); disco massimo 360 MB su 10 GB; volume letto da S3 fino a 814 MiB (non misurato il traffico effettivo).
- Errori con output; prove saltate o interrotte:
  - `seed42` tentativo 1 (Kaggle v1, ~10 min GPU): `ModuleNotFoundError: nrrd` alla costruzione del modello — `koine_machines` importa `vesuvius` solo a quel punto; preflight insufficiente (importava solo l'entry point).
  - `seed42` tentativo 2 (v2, ~13 min GPU): inferenza completata (6.458 blocchi in 204 s), fallito alla scrittura del TIFF: `COMPRESSION.LZW requires the 'imagecodecs' package` (import pigro di `tifffile`). Store intermedi cancellati a fine run: risultato ricalcolato al tentativo 3.
  - Preflight v1–v8 senza GPU: 4 fallimenti informativi (torch `+cpu` sull'immagine CPU; `hf buckets sync` bloccato sui 5.128 file; dataset non montato e HTTP 429 sul download diretto; kernel del notebook che non vede i pacchetti editable) — tutti registrati nel piano.
  - **Tracciabilità dei tentativi falliti:** i log grezzi delle versioni fallite sono stati sovrascritti in locale dal download della versione successiva (la cartella `runs/E00-R01/<run>/` conteneva solo l'ultima versione); restano consultabili nella cronologia delle versioni dei notebook su Kaggle (`seed42` v1 e v2, `seed43` v1, `preflight` v1–v3, v6). Le cause riportate qui sono quelle lette in quei log al momento del fallimento. Dalla prossima esecuzione gli output vanno scaricati in cartelle per versione.
- Controlli di falsi positivi e limiti: il test di orientamento è il controllo contro un falso "superato" da assi sbagliati (le tre trasformazioni scendono a ~0,53–0,58, prossime al caso). Nessuna calibrazione: i valori del TIFF non sono probabilità.
- Valutazione indipendente del socio e ripetizione: non ancora (E01).
- Conclusione circoscritta: **la catena dati → codice → modello → output è collegata correttamente su un segmento del training set, con orientamento verificato** (livello L3). La smentirebbe: un TIFF diverso a parità di commit, checkpoint e input; oppure un E01 del socio con AUROC o orientamento diversi.
- Decisione successiva e motivazione: **promuovere a E01** (ripetizione del socio dal solo repository). L'ambiente del socio (Mac, `uv run`) differirà: dichiararlo nel piano E01 come variabile.

### Deviazioni dichiarate rispetto al comando ufficiale

1. Installazione su Python di sistema con `--no-deps` invece di `uv run` (decisione di Matteo; costo osservato: due tentativi GPU persi su dipendenze pigre, ora coperte da prove CPU nel preflight).
2. Label non sincronizzata con `hf buckets sync` (inadatto ai file piccoli) ma scaricata dal bucket con download diretto a pochi thread (HTTP 429 sul download parallelo); nei run GPU questo è stato il percorso effettivo, mentre il dataset Kaggle privato, previsto come primario, ha funzionato solo nel preflight (percorso di montaggio diverso nei run GPU). Provenienza verificata per contenuto in tutti i run.
3. `--no-compile`, `--gpus 0`, `--batch-size 1`, `CUDA_VISIBLE_DEVICES=0` (preregistrati).
4. Allocazione Kaggle a due T4 con uso di una sola (unica opzione T4 disponibile); mancato uso della seconda misurato.

### Seed 43 (informativo)

Tentativo 1 (Kaggle v1): fermato dalla guardia iniziale prima di qualunque GPU (~1 min): l'output del seed 42 collegato come `kernel_source` non era visibile al percorso atteso; risolto pubblicando quell'output anche come dataset Kaggle privato e verificandone lo SHA-256 nella guardia. Tentativo 2 (v2, 14:40–15:09 UTC): **completato**, 24 file verificati.

| Misura | seed 43 | seed 42 (riferimento) |
|---|---|---|
| AUROC, label originale | **0,99825** | 0,99908 |
| AUROC rot180 / flipY / flipX | 0,511 / 0,537 / 0,581 | 0,532 / 0,540 / 0,576 |
| Orientamento | corretto | corretto |
| Mediana inchiostro / sfondo | 200 / 58 | 200 / 57 |
| gate_B | superato | superato |
| Inferenza | **619 s** | 200 s |
| Picco GPU 0 / GPU 1 | 383 / 3 MiB | 383 / 3 MiB |
| SHA-256 TIFF | `72fabca9…c8e5` | `a0cc3f53…3c38` |

**Concordanza fra i seed** (sui 1.071.121 pixel supervisionati): correlazione di Spearman **0,917**; ΔAUROC (43 − 42) = **−0,0008**. Il risultato è concordante anche nel secondo seed testato; non si inferisce stabilità oltre questi due checkpoint, e non era preregistrata alcuna soglia. Come dichiarato prima della prova, la concordanza **non** è indipendenza (stessa architettura, stessi dati, stessa ricetta) e non cambia il verdetto.

Nota sul tempo: l'inferenza del seed 43 ha impiegato 619 s contro i 200 s del seed 42, con configurazione identica nei log. La causa non è determinata dagli artefatti disponibili. Il tetto di 30 minuti resta rispettato; i tempi su Kaggle non sono riproducibili per contratto.

Conservare anche gli esiti negativi. Non sostituire valori mancanti con zero o stime presentate come misure.
